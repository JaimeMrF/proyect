// const {user, mayor} = require("./module/myModule");

// console.log(`${user} es mayor?: ${mayor}`);

const math = require("./math/index");

console.log(
    math.suma(4,5),
    math.resta(4,5),
    math.division(4,5),
    math.multiplicacion(4,5)
)